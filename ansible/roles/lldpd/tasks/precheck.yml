---
- name: Precheck lldpd configuration
  ansible.builtin.import_role:
    name: service-precheck
  vars:
    service_precheck_services: "{{ lldpd_services }}"
    service_name: "{{ project_name }}"
